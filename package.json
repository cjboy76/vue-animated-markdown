{
  "name": "vue-animated-markdown-monorepo",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@10.12.1",
  "scripts": {
    "dev": "pnpm --filter playground dev",
    "build": "pnpm run -r --filter=!playground build",
    "build:watch": "pnpm run -r --filter=!playground build:watch"
  }
}
